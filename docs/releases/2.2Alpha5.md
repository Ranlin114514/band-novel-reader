# Band Novel Reader 2.2Alpha5

本版将应用版本信息集中管理，关于页显示与更新检查基准共享同一版本元数据，发布构建前可统一校验版本标签、版本名称和版本号，避免版本信息不同步。

首次引导新增可选的 AI 总结配置步骤。用户可直接进入 AI 设置，填写 API 地址与 API Key、读取服务支持的模型、选择或手动填写模型 ID，并测试模型服务连通性；未配置 AI 不影响小说导入、分段和通知发送。

设置中新增实验室入口。进入前会显示高风险提示，实验室当前提供分段规则试验场和更新提醒策略诊断，所有实验结果仅用于查看，不会自动写入书库或修改现有设置。
