# GitHub 历史发行版审计记录

审计时间：2026-08-25（GMT+8）

本次在创建 `2.2Alpha1` 之前，对仓库 `Ranlin114514/band-novel-reader` 的 GitHub Releases、标签和附件进行了核查。审计结果显示共有 **15 个**既有发行版，所有既有发行版均保留，且每个发行版都恰好包含一个名为 `band-novel-reader.apk` 的附件。

| 项目 | 审计结果 |
| --- | --- |
| 既有发行版数量 | 15 |
| 既有版本标签 | `v0.1alpha` 至 `2.2Alpha`，含 `2.1` 与 `v2.1Alpha` |
| 每个既有发行版 APK 附件数 | 1 |
| 既有 APK 附件文件名 | `band-novel-reader.apk` |
| 历史发行版缺失 | 未发现 |

> GitHub 的 Releases 页面可能会因“Latest”标记、预发布筛选或页面展示顺序而使早期测试版不显眼；本审计确认历史发行记录和 APK 附件均仍存在。本次不会改名、覆盖或删除任何既有发行版。

下一次上传仅新增独立预发布 `2.2Alpha1`，其后续新迭代才使用 `2.2Alpha2`。
