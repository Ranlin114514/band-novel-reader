# 一言启动页接口说明

启动页使用一言公开接口 `https://v1.hitokoto.cn/?c=i&encode=json` 请求一句文案。已核验的 JSON 响应包含 `hitokoto`（正文）、`from`（出处）与 `from_who`（作者）字段。应用为请求设置短超时；网络不可用、接口异常或字段为空时，将显示本地兜底句子并继续进入书库，不会阻塞启动。

用户可在“统一设置管理”的“启动体验”中关闭启动页。关闭后，应用直接进入书库且不发起一言请求。
